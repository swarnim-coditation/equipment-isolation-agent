info:
  name: get run trace
  type: http
  seq: 7

http:
  method: GET
  url: "{{BASE_URL}}/isolation-runs/{{RUN_ID}}/trace"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
