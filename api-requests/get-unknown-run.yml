info:
  name: get unknown run
  type: http
  seq: 14

http:
  method: GET
  url: "{{BASE_URL}}/isolation-runs/not-a-real-run-id"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
